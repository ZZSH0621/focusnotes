# PRD · Focus Notes 2.0 · 体验升级版

## Summary

Focus Notes 当前版本已具备任务管理的核心能力（状态、优先级、MD 导出），但在使用体验上缺乏亮点：界面以静态交互为主，功能深度不足以形成用户粘性。本文档定义 2.0 版本的产品方向——以「有仪式感的专注工具」为主题，通过引入专注计时、拖拽看板、全局快速捕捉、每日进度统计、账号体系与云同步，以及覆盖全局的物理感动效体系，使 Focus Notes 从一个轻量记事工具升级为有节奏感、多设备可用的个人专注助手。

## Goals

- 建立差异化体验：让用户在首次打开时就感受到产品「有用心」的质感（启动动画 + 弹性交互）。
- 形成完整的专注闭环：任务创建 → 专注计时 → 完成归档 → 每日复盘，每个环节都有明确的交互反馈。
- 降低任务捕捉摩擦：全局快捷键让用户无需切换窗口即可记录想法，尤其适合桌面端场景。
- 为跨平台做交互基础：动效体系和组件设计考虑移动端触控适配，为后续 Tauri 移动端构建打下基础。
- 实现多设备数据一致：用户在任意平台添加或完成的任务，其他平台实时反映，彻底消除「只能用一台设备」的局限性。

## Users and Needs

核心用户是在电脑前长时间工作的个人用户（学生、知识工作者、自由职业者），他们的共同诉求是：

- **快速记录**：脑子里冒出来的事情，不想打断当前节奏却又怕忘。
- **有节奏地工作**：需要工具帮助分配注意力，而不只是堆砌任务列表。
- **有成就感的完成**：做完一件事需要有「结束感」，单纯的勾选不够。
- **视觉上舒适**：不喜欢笨重的企业软件，但也不满足于纯静态的纯文字界面。

## User Stories, Features and Requirements

### 1. 启动界面动画

用户打开应用时，看到有质感的启动动画（Logo 渐入 + 元素依次出现），建立对产品的第一印象。

- 启动动画时长控制在 1.2 秒以内，不阻塞使用。
- 动画结束后自动过渡到主界面，无需用户操作。
- 支持跳过（长按或点击可提前进入）。
- Web 端和 Tauri 桌面端均需实现；移动端后续同步。

### 2. 全局物理感动效体系

用户在与任务卡片、按钮、弹层等元素交互时，感受到有弹性、有惯性的物理反馈，而不是生硬的瞬间切换。

- 任务卡片添加：从输入区以弹性曲线滑入列表（spring animation，刚度 300，阻尼 25）。
- 任务卡片删除：向右侧滑出消失，带速度衰减。
- 任务完成：打勾时描边动画 + 短暂的粒子扩散（confetti），卡片文字同步划线过渡。
- 优先级切换：颜色以 200ms 过渡，标签有轻微弹跳反馈。
- 按钮 hover / active 状态：轻微缩放（scale 0.97）+ 阴影变化。
- 所有动效需遵循系统级「减少动态效果」偏好设置（prefers-reduced-motion）。

### 3. 拖拽看板视图

用户可以在看板模式下，通过拖拽任务卡片在「进行中」和「已完成」列之间流转，取代纯按钮操作。

- 看板视图作为列表视图的可切换视图（顶部 Tab 或图标切换）。
- 拖拽过程中目标列高亮，卡片有跟手的物理感（跟随鼠标 / 手指）。
- 放置时触发对应状态变更，并有入场动画。
- 移动端支持触摸拖拽（为 Tauri 移动端预留）。
- 拖拽期间不触发误操作（需区分拖拽意图和点击意图，建议位移阈值 &gt; 5px 才激活拖拽）。

### 4. 专注计时器（番茄钟）

用户可以为任务绑定一个专注计时器，进入专注模式后界面聚焦于当前任务，计时结束有通知提醒。

- 默认时长 25 分钟，用户可自定义（5 分钟步进，范围 5–60 分钟）。
- 计时中：顶部或任务卡片显示倒计时进度条，其余 UI 降低亮度（专注态）。
- 计时结束：系统通知 + 应用内提示；桌面端通过 Tauri 调用原生通知。
- 支持暂停 / 继续 / 放弃计时。
- 每次完成计时记录为一个「番茄」，数据写入当日统计。
- Web 端通过 Page Visibility API 处理后台计时（标签切换时继续计时）。

### 5. 每日进度统计

用户在主界面可以看到今日任务完成情况的可视化呈现，形成每日工作的成就感闭环。

- 展示当日：完成任务数 / 总任务数、完成的番茄数、专注总时长。
- 进度以环形图或线性进度条呈现，颜色随完成率变化（渐变色或里程碑颜色）。
- 数据按自然日重置，历史数据保留（可查看过去 7 天趋势，次要功能）。
- 统计数据存储于 IndexedDB（Web）/ Rust 本地文件（桌面端），与任务数据分表存储。

### 6. 全局快速捕捉（桌面端）

用户在使用其他应用时，通过全局快捷键唤出 Focus Notes 的悬浮输入框，快速记录任务，不打断当前工作流。

- 默认快捷键：`Cmd/Ctrl + Shift + N`，用户可自定义。
- 悬浮输入框：居中弹出，支持输入标题 + 优先级选择，回车提交。
- 提交后输入框自动关闭，任务写入 IndexedDB / 本地文件。
- 通过 Tauri v2 全局快捷键 API（`tauri-plugin-global-shortcut`）实现。
- Web 端不支持此功能（可展示引导提示推荐使用桌面版）。

### 7. 账号体系与云同步

用户注册登录后，任务数据与账号绑定，实现多平台（Web、桌面端、后续移动端）数据自动同步，换设备不丢失数据。

- 认证方式：邮笱 + 密码注册，后续可考虑接入 Google / GitHub OAuth。
- 本地优先策略（Offline First）：断网时数据写入本地，联网后自动增量同步。
- 冲突解决策略：以最后修改时间为准（Last Write Wins），单条任务级别冗余。
- 同步状态指示：界面上显示「同步中 / 已同步 / 同步失败」状态，失败时提供重试入口。
- 数据传输全程 HTTPS 加密；服务端存储加密方案待评估。
- 未登录用户仍可使用本地模式，登录后可选择将本地数据迁移至账号。

## Success Metrics

- **留存**：7 日留存率较 1.0 提升 20%（基线待 1.0 数据确认）。
- **专注功能采用率**：新增用户中 40% 在首周内使用过番茄钟至少一次。
- **快速捕捉使用频率**：桌面端 DAU 中，使用全局快捷键的比例 &gt; 30%。
- **性能**：所有动效在目标设备上帧率维持 60fps，启动动画不延迟首屏可用时间超过 1.5 秒。
- **用户反馈**：应用商店 / 用户调研中「界面体验」评分提升。

## Risks and Open Questions

- **动效性能**：物理感动效在低配设备（尤其移动端）上的性能表现需要实测，可能需要按设备能力分级降级。
- **拖拽与触控兼容**：桌面拖拽和移动端触摸拖拽逻辑差异较大，需要选择合适的库（如 dnd-kit）或自行实现，避免两套代码分叉。
- **番茄钟后台计时**：Web 端 Service Worker 计时精度问题，以及 Tauri 窗口最小化时的计时一致性，需要验证。
- **全局快捷键冲突**：用户系统中可能已有其他应用占用默认快捷键，需要提供冲突检测和自定义入口。
- **每日数据重置时区**：统计重置应以用户本地时区为准，需确认 IndexedDB 和 Rust 端的时区处理一致性。
- **云同步服务选型**：后端服务选择（自建 vs Supabase / Firebase 等 BaaS）需尽早确定，不同选择对实时同步能力、冲突处理精度、运维成本影响显著。
- **数据迁移与兼容**：已有的 IndexedDB / 本地文件数据需要平滑迁移至云端数据模型，数据结构必须向前兼容。
- **离线冲突复杂度**：Last Write Wins 对大多数场景足够，但如果用户在两台设备同时修改同一任务，可能丢失部分编辑——需向用户明确告知此限制。
- **认证安全**：登录流程涉及 Token 管理、刻新机制、密码重置等，工程量不小，应列入独立排期评估。
- **隐私合规**：涉及用户数据上云后，需明确数据存储地域和隐私政策，尤其面向欧盟 / 国内用户时不可忽视。

